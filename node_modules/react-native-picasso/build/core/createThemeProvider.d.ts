import * as React from 'react';
import { Theme } from '../styles/defaultTheme';
interface Props {
    theme: Theme;
}
declare function createThemeProvider(defaultTheme: Theme, ThemeContext: React.Context<Theme>): React.FC<Props>;
export default createThemeProvider;
